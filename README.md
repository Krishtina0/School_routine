# School_routine
I made the routine table using HTML by using &lt;table>, &lt;tr>, &lt;th> and &lt;td>tags, along with attributes like border, rowspan, colspan, cellpadding and cellspacing,. I also used simple HTML alignment tags to keep the headings neat and the layout clear. 
